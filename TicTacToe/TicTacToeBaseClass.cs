namespace TicTacToe;

public abstract class TicTacToeBaseClass
{
    private int Size; //used to set the size of the grid. potentially variable if we use larger grids for our game
    private (int, int) [] Locations; // read about get/set. is that a better way of doing this?
    //public abstract void behaviour(); //CHECK FORMAT FOR THIS

    public void GridLayout() //potentially make this into a new class?
    {
        
    }
}